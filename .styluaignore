# Luau definition file: stylua's parser (full-moon) handles Luau
# *scripts* but not Mode::Definition syntax (`declare name: type`),
# which is exactly what this prelude uses. It is checked by the
# check-stage Luau gate instead.
src/shuttle-prelude.d.luau
